#!bash
# Copy tables from one database to another locally

# Usage: strat-copy-local <source_db> <target_db> <table_name>

dumpfile=$1
shift

strat compose exec -T postgres pg_dump -Fc -U postgres $@ > $dumpfile