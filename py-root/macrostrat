#!/usr/bin/env bash

# This command is deprecated, go to the actual one

BIN_DIR="$(cd "$(dirname "$(readlink "$0")")" && cd ../bin && pwd)"

export MACROSTRAT_SHOULD_REINSTALL=1
$BIN_DIR/macrostrat "$@"

