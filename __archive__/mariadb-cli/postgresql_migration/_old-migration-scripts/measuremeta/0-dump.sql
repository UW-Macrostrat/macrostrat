-- This one is executed on MariaDB
SELECT
  id,
  sample_name,
  lat,
  lng,
  sample_geo_unit,
  sample_lith,
  lith_id,
  lith_att_id,
  age,
  early_id,
  late_id,
  sample_descrip,
  ref,
  ref_id
FROM measuremeta
